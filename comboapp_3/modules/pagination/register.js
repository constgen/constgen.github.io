Core.register('pagination', function (sandbox) {
	return (function (root) {
		if (!root) return
		var module = {
			init: function () {
				
			}

		}
		return module
	}(document.querySelectorAll('.pagination'))) //Node element
})
