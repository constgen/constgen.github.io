Core.register('mediakitMenu', function (sandbox) {
	return (function (root) {
		if (!root) return
		var module = {
			init: function () {
				
			},

			listen: {
				
			}

		}
		return module
	}(document.querySelector('#mediakitMenu')))
})
