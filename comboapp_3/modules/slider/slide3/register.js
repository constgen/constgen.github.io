Core.register('slide3', function (sandbox) {
	return (function (root, $) {
		if (!root) return
		var module = {
			init: function () {
				
			},

			listen: {
				
			}

		}
		return module
	}(document.querySelector('.slide3'), window.jQuery))
})
