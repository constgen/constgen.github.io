Core.register('careersWhyIntersog', function (sandbox) {
	var root, module;
	// root HTML element
	if (!(root = document.querySelector('.careersWhyIntersog'))) return;
	// module
	var module = {
		init: function () {

		},

		destroy: function () {

		},

		//switchable styles
		css: '',

		listen: {
			'app-load': function () {

			}
		}

	}
	// return module object
	return module;
})

